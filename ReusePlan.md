# 긱온 기존 사이트 재사용 전략

> 작성일: 2026-05-14
> 목적: 현재 구현된 React 사이트를 버리지 않고, 새 기획에 맞게 어떤 부분을 살리고 바꿀지 정리한다.

---

## 1. 기본 판단

기존 사이트는 폐기하지 않는다.

React, Vite, TypeScript, Supabase 연동, 인증, 프로필, 전문가 상세, 패키지 카드 등 기본 뼈대가 이미 있으므로 새로 만드는 것보다 **기존 구조를 리브랜딩하고 흐름을 바꾸는 방식**이 효율적이다.

다만 현재 사이트는 “검증된 최고의 AI 전문가” 느낌과 요청 게시판 흐름이 강하다. 새 기획은 다음 방향으로 바뀌었다.

```text
누구나 AI 전문가가 될 수 있고,
누구나 저렴하게 AI 외주를 의뢰할 수 있다.
```

따라서 메시지, 카테고리, CTA, 거래 흐름을 새 기획에 맞게 수정한다.

---

## 2. 재사용할 것

- React/Vite/TypeScript 프로젝트 구조
- 라우팅 구조
- Supabase 클라이언트 구조
- 로그인/회원가입
- 온보딩
- 마이페이지 기본 구조
- 전문가 프로필 작성 구조
- 전문가 상세 페이지 구조
- 패키지 카드 구조
- 서비스 요청 작성 폼 일부
- 요청 게시판의 목록/상세 모달 일부
- 전체 카드형 UI와 CSS 변수 기반 디자인 시스템

---

## 3. 바꿀 것

### 3.1. Home

기존 Home은 유지하되 문구와 섹션을 새 기획에 맞게 바꾼다.

변경:

- 메인 문구를 `AI 외주를 더 쉽고 저렴하게`로 변경
- 10개 카테고리를 초기 3개 카테고리 중심으로 변경
  - AI 영상/숏폼
  - AI 이미지/캐릭터
  - AI 개발/자동화
- `실시간 프로젝트 요청` 섹션 비중 축소 또는 제거
- `바로 의뢰할 수 있는 AI 작업` 섹션 추가
- `AI 전문가로 시작하기` 섹션 추가
- 작업 진행표 소개 추가

### 3.2. Category

기존 Category는 전문가 목록 중심이다. 새 기획에서는 **상품 목록 중심**으로 바꾼다.

변경:

- 페이지 이름/역할을 전문가 탐색에서 상품 탐색으로 전환
- 카테고리 필터를 초기 3개로 축소
- 상품 카드 중심으로 표시
- 가격/사용 AI 도구 필터 추가
- 평점/리뷰 정렬은 나중으로 미룸

### 3.3. ExpertDetail

기존 ExpertDetail은 전문가 공개 프로필에 가깝다. 새 기획에서는 **전문가/상품 상세** 역할로 확장한다.

변경:

- 상품 제목/샘플 결과물/사용 AI 도구를 상단에 더 강하게 표시
- 패키지 카드의 `주문하기`를 `패키지로 의뢰하기`로 변경
- 버튼 클릭 시 바로 결제가 아니라 요구사항 작성으로 이동
- 리뷰/평점 하드코딩 제거 또는 약화
- 작업 진행 방식 안내 추가

### 3.4. ServiceRequest

기존 ServiceRequest는 요청서 작성 페이지다. 새 기획에서는 **패키지 의뢰/요구사항 작성 페이지**로 재사용한다.

변경:

- 선택한 패키지 요약 영역 추가
- 원하는 결과물/참고자료/마감일 중심으로 간소화
- 단일/단계별 진행 희망 선택 추가
- AI 작업 체크리스트 추가
- 연락처 입력은 제거
- 제출 후 거래 제안서 대기 상태로 이동

### 3.5. RequestBoard

기존 RequestBoard는 숨고식 요청 게시판에 가깝다.

새 기획에서는 요청 게시판이 메인 흐름이 아니므로, 초기 런칭에서는 비중을 낮춘다.

처리:

- 메인 네비게이션에서는 제거하거나 보조 메뉴로 이동
- 기존 코드는 나중 기능으로 보관
- 일부 UI는 의뢰 요청 목록/마이페이지에 재사용 가능

### 3.6. Profile

기존 Profile은 전문가 프로필 편집 기능이 있다.

새 기획에서는 전문가 상품 등록까지 확장한다.

변경:

- 사용 AI 도구와 샘플 결과물 입력 강화
- Standard 패키지 필수 등록
- Deluxe/Premium 선택 등록
- 상품명/카테고리/샘플 결과물 등록 추가
- 전문가 최소 기준 검증 추가

### 3.7. MyPage

기존 MyPage는 프로필 조회 중심이다.

새 기획에서는 개인 활동 관리 페이지로 확장한다.

변경:

- 의뢰자 섹션: 의뢰 요청, 받은 제안서, 진행 중 작업
- 전문가 섹션: 등록 상품, 받은 요청, 보낸 제안서, 진행 중 작업
- 상품 등록/수정 진입 버튼 추가

### 3.8. ChatModal

기존 ChatModal은 목업 자동응답이다.

초기 런칭에서는 실시간 채팅을 제외하므로, 메인 기능으로 사용하지 않는다.

처리:

- 문의하기는 간단한 내부 메시지/요청 기록으로 대체
- ChatModal은 나중 실시간 채팅 기능으로 보류

---

## 4. 새로 만들 것

기존 구조만으로 부족한 페이지/기능이다.

- 상품 데이터 구조
- 상품 카드 컴포넌트
- 패키지 의뢰/요구사항 작성의 패키지 요약
- 거래 제안서 작성/확인 페이지
- 작업 진행표 페이지
- 산출물 링크 제출 기능
- 제안서 유효기간/만료 상태

---

## 5. 제거 또는 보류할 것

초기 런칭에서는 우선순위를 낮춘다.

- 요청 게시판 메인 흐름
- 실시간 채팅
- 자동 결제
- 에스크로
- 단계별 자동 정산
- 리뷰/평점 고도화
- 전문가 등급/랭킹
- 커뮤니티

---

## 6. 결론

기존 사이트는 폐기하지 않는다.

기존 구현은 다음처럼 재사용한다.

```text
Home → 새 메시지와 3개 카테고리로 리브랜딩
Category → 전문가 목록에서 상품 목록으로 개편
ExpertDetail → 전문가/상품 상세로 확장
ServiceRequest → 패키지 의뢰/요구사항 작성으로 재사용
Profile → 전문가 상품 등록으로 확장
MyPage → 의뢰/제안/작업 관리로 확장
RequestBoard/ChatModal/Community → 초기 메인 흐름에서 제외
```
