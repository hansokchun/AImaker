
# Blueprint: AIConnect

## 1. 개요

AIConnect는 사용자와 다양한 분야의 전문가를 연결해주는 현대적인 웹 애플리케이션입니다. 깔끔하고 직관적인 인터페이스를 통해 전문가를 검색, 탐색하고 연결할 수 있는 플랫폼을 제공합니다.

## 2. 스타일, 디자인 및 기능 (업데이트 버전)

### 2.1. 디자인 시스템

*   **타이포그래피**:
    *   `clamp()` 함수를 사용하여 화면 크기에 따라 폰트 크기가 부드럽게 변하는 유동적 타이포그래피(Fluid Typography)를 적용했습니다.
    *   데스크톱(PC) 환경에서 글자가 겹치거나 잘리지 않도록 줄 간격(line-height)과 자간(letter-spacing)을 최적화했습니다.
*   **색상 및 시각 효과**:
    *   브랜드 블루 컬러를 중심으로 한 고대비 배색을 사용했습니다.
    *   글래스모피즘(Glassmorphism) 효과를 내비게이션 바에 적용하여 세련된 느낌을 주었습니다.
*   **레이아웃**:
    *   `1280px` 기반의 넓은 컨테이너 레이아웃을 적용하여 대화면 PC에서도 여유로운 시각적 경험을 제공합니다.
    *   그리드(Grid) 시스템을 개선하여 카드 간의 간격과 크기 비율을 고정된 값 대신 유연한 비율로 조정했습니다.

### 2.2. 핵심 기능

*   **반응형 내비게이션**: PC 환경에서 메뉴 간격을 넓히고 가독성을 높였습니다.
*   **고도화된 히어로 섹션**: 검색 바의 포커스 효과와 입체감을 강화하고, 배지 애니메이션이 텍스트와 겹치지 않도록 위치 비율을 조정했습니다.
*   **전문가 카드 (Web Components)**: 그림자와 라운드 처리를 강화하고, 이미지 비율을 최적화하여 정보 전달력을 높였습니다.
*   **실시간 채팅 시스템 (문의하기)**: 전문가 상세 페이지에서 '문의하기' 버튼을 통해 즉각적인 상담이 가능한 채팅 UI를 제공합니다. 자동 응답 로직을 포함하여 실시간 소통 경험을 시뮬레이션합니다.

## 3. 변경 이력

*   **2026-04-09**: 실시간 채팅 기능 추가. 전문가 상세 페이지의 '문의하기' 버튼과 연동된 채팅 UI 및 로직 구현.
*   **2026-04-07**: 전체 사이트 UI 비율 및 글자 겹침 현상 수정. PC 환경 최적화 레이아웃 적용. `clamp()` 기반의 유동적 디자인 도입.
