# Lessons

## 2026-06-17 작업방 예외상황 정책 구현

- 취소 상태는 정산 상태와 분리해야 한다. `settlementStatus: refunded`로 바로 바꾸면 실제 환불 완료처럼 보이므로, 첫 런칭에서는 `refundStatus: fee_excluded_refund_pending`처럼 검토/처리 예정 상태를 별도로 둔다.
- 수정 요청은 제안서의 `revisionCount`를 작업 생성 시점에 복사해 `revisionLimit`으로 고정해야 한다. 이후 작업방에서는 `revisionUsed`만 증가시켜 거래 중 조건이 흔들리지 않게 한다.
- 작업방 예외 정책을 바꿀 때는 `storage`, `Workroom`, 관련 테스트, 정책 문서를 함께 갱신해야 한다.
- 기존 테스트 mock이 새 저장소 export를 모르면 화면이 로딩 상태에 멈출 수 있다. 작업방 의존성이 늘어나면 `WorkroomReturn` 같은 주변 테스트 mock도 같이 확인한다.

## 2026-06-17 상품/프로필 신뢰 정보 표시

- 상품 상세에 보여줄 판매자 신뢰 정보는 `profiles`만으로 부족하다. 연락 가능 시간, 평균 응답 시간처럼 전문가 전용 정보는 `expert_profiles`의 `getStoredProfile`까지 같이 읽어 병합해야 한다.
- 작업방 진행표는 제안서 마일스톤을 그대로 보여주면 의뢰자가 현재 거래 상태를 이해하기 어렵다. 런칭 초기에는 `흐름설계 → 결과물 제출 → 결과물 승인 및 정산`처럼 고정 단계와 상태 라벨을 보여주는 편이 더 명확하다.
- `0명 관심`처럼 초기 서비스의 빈 숫자를 드러내는 UI는 신뢰를 낮출 수 있다. 카운트가 제품 가치를 증명하지 못하는 초기에는 숨기는 편이 낫다.
