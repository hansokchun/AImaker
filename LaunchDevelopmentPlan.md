# AIConnect 런칭 개발 계획

> 작성일: 2026-05-14
> 목적: 현재 기획을 실제 개발 순서로 바꾸기 위한 런칭 전 작업 계획이다.

---

## 1. 개발 목표

첫 런칭 목표는 완전한 자동 거래 플랫폼이 아니라, 다음 흐름이 실제로 동작하는 AI 외주 마켓이다.

```text
전문가가 AI 상품을 등록한다.
의뢰자가 상품을 보고 요구사항을 보낸다.
전문가가 거래 제안서를 보낸다.
의뢰자가 제안서를 승인한다.
작업 진행표에서 산출물을 확인한다.
```

자동 결제, 에스크로, 단계별 정산은 첫 런칭 이후로 미룬다.

---

## 2. 개발 단계 요약

초기 개발은 5단계로 나눈다.

```text
1단계: 데이터와 타입 정리
2단계: 탐색 화면 개편
3단계: 의뢰/제안 흐름 구현
4단계: 작업 진행표와 산출물 제출 구현
5단계: 마이페이지와 런칭 점검
```

---

## 3. 1단계: 데이터와 타입 정리

목표:

- 기존 전문가 중심 데이터를 상품 중심 데이터로 바꾼다.
- mock data로 전체 흐름을 먼저 검증한다.
- Supabase 연결 전에도 화면 개발이 가능하게 만든다.

작업:

- `src/types/index.ts`에 상품, 패키지, 의뢰 요청, 제안서, 작업 진행표 타입 추가
- `src/data/mockData.ts`를 AI 영상/숏폼, AI 이미지/캐릭터, AI 개발/자동화 기준으로 재구성
- 카테고리 상수를 `src/constants/categories.ts`로 분리
- 정책 상수를 `src/constants/policies.ts`로 분리

완료 기준:

- 상품 카드, 상품 상세, 의뢰 요청, 제안서, 작업 진행표가 같은 mock data 흐름으로 연결된다.

---

## 4. 2단계: 탐색 화면 개편

목표:

- 사용자가 AIConnect를 “AI 작업을 저렴하게 맡기는 곳”으로 이해하게 만든다.
- 전문가 목록보다 상품 목록을 먼저 보게 만든다.

작업:

- 홈 문구를 `AI 외주를 더 쉽고 저렴하게` 방향으로 수정
- 홈에 3개 초기 카테고리 노출
- 홈에 추천 AI 작업 상품 노출
- `Category.tsx`를 상품 카드 중심으로 개편
- `ExpertDetail.tsx`를 상품 상세에 가깝게 개편
- `PackageCard.tsx`에서 `패키지로 의뢰하기` CTA를 요구사항 작성 페이지로 연결

완료 기준:

- 의뢰자가 홈에서 카테고리 또는 상품을 눌러 요구사항 작성까지 이동할 수 있다.

---

## 5. 3단계: 의뢰/제안 흐름 구현

목표:

- 바로 결제하지 않고, 요구사항 작성 후 전문가가 제안서를 보내는 흐름을 만든다.

작업:

- `ServiceRequest.tsx`를 패키지 의뢰/요구사항 작성 화면으로 개편
- 연락처 입력 제거
- 원하는 결과물, 작업 목적, 참고자료, 마감일, 진행 방식 선택 입력 추가
- 신규 `Proposal.tsx` 페이지 추가
- 제안서 상태 추가
  - `sent`
  - `revision_requested`
  - `accepted`
  - `cancelled`
  - `expired`
- 제안서 3일 만료 표시
- 승인, 수정 요청, 취소 버튼 추가

완료 기준:

- 의뢰자가 요구사항을 보내고, 전문가가 보낸 제안서를 확인한 뒤 승인 또는 수정 요청을 할 수 있다.

---

## 6. 4단계: 작업 진행표와 산출물 제출 구현

목표:

- 의뢰자가 작업이 어디까지 진행됐는지 확인할 수 있게 한다.
- 대용량 파일은 외부 링크 중심으로 처리한다.

작업:

- 신규 `Workroom.tsx` 페이지 추가
- 단일 진행/단계별 진행 상태 표시
- 단계 상태 추가
  - `waiting`
  - `in_progress`
  - `submitted`
  - `revision_requested`
  - `approved`
- 전문가 산출물 링크 제출 UI 추가
- 의뢰자 승인/수정 요청 UI 추가
- 소형 파일 URL과 외부 링크를 함께 표시할 수 있게 타입 구성

완료 기준:

- 제안서 승인 후 작업 진행표가 생성되고, 전문가가 산출물 링크를 제출하며, 의뢰자가 승인 또는 수정 요청을 할 수 있다.

---

## 7. 5단계: 마이페이지와 런칭 점검

목표:

- 의뢰자와 전문가가 자신의 거래 상태를 한눈에 볼 수 있게 한다.

작업:

- `MyPage.tsx`를 역할별 섹션으로 개편
- 의뢰자 섹션
  - 내 의뢰 요청
  - 받은 제안서
  - 진행 중인 작업
  - 완료된 작업
- 전문가 섹션
  - 내가 등록한 상품
  - 받은 의뢰 요청
  - 보낸 제안서
  - 진행 중인 작업
- 상품 등록 화면을 `ProductForm.tsx`로 분리하거나 `Profile.tsx`에서 단계적으로 분리
- 완료된 작업에만 리뷰 작성 버튼 표시

완료 기준:

- 의뢰자와 전문가가 마이페이지에서 다음 행동으로 이동할 수 있다.

---

## 8. 테스트 계획

현재 저장소에는 테스트 도구가 설정되어 있지 않다. 구현 전 다음 중 하나를 선택해야 한다.

추천:

```text
Vitest + React Testing Library
```

우선 테스트할 항목:

- 상품 카드에 가격, AI 도구, 샘플 정보가 보인다.
- 요구사항 작성 화면에 연락처 입력이 없다.
- 제안서는 3일 후 만료 상태가 된다.
- 제안서 승인 전에는 작업 진행표가 열리지 않는다.
- 산출물은 외부 링크 중심으로 제출된다.
- 완료된 작업에만 리뷰 작성 버튼이 보인다.

---

## 9. Supabase 적용 순서

초기 화면 흐름이 mock data로 안정되면 Supabase를 붙인다.

적용 순서:

1. `profiles`
2. `expert_products`
3. `service_requests`
4. `proposals`
5. `works`
6. `deliverables`
7. `reviews`

각 테이블은 RLS를 함께 작성한다. 특히 작업 진행표, 제안서, 산출물은 의뢰자와 전문가만 볼 수 있어야 한다.

---

## 10. 런칭 전 체크리스트

- 홈에서 핵심 메시지가 5초 안에 이해되는가
- 상품 탐색에서 가격, 샘플, AI 도구가 바로 보이는가
- 요구사항 작성이 복잡하지 않은가
- 사적 연락처 입력란이 없는가
- 제안서 승인 전 작업 시작 흐름이 막혀 있는가
- 작업 진행표에서 현재 상태가 명확한가
- 대용량 산출물은 외부 링크로 처리되는가
- 모바일 화면에서 주요 CTA가 잘 보이는가
- 비어 있는 카테고리에서도 사용자가 다음 행동을 할 수 있는가

---

## 11. 기획 단계에서 남은 일

현재 큰 기획은 대부분 정리됐다. 남은 기획은 개발 직전에 결정해야 하는 실행형 기획이다.

1. 화면별 와이어프레임
   - 홈
   - 상품 목록
   - 상품 상세
   - 요구사항 작성
   - 제안서
   - 작업 진행표
   - 마이페이지

2. 실제 화면 문구
   - 버튼 문구
   - 빈 상태 문구
   - 오류 문구
   - 제안서/작업 상태 안내 문구

3. 테스트 도구 선택
   - Vitest + React Testing Library를 도입할지 결정

4. Supabase 상세 설계
   - 실제 컬럼명
   - RLS 정책
   - Storage bucket 정책

5. 시각 디자인 방향
   - 컬러
   - 카드 스타일
   - 버튼 스타일
   - 모바일 레이아웃

---

## 12. 결론

이제 기획은 “무엇을 만들지”에서 “어떤 화면부터 어떤 순서로 만들지”로 넘어왔다.

다음으로는 화면별 와이어프레임을 잡는 것이 좋다. 와이어프레임이 정리되면 바로 TDD 기반 구현 단계로 넘어갈 수 있다.
