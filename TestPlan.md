# AIConnect 테스트 전략 문서

> 작성일: 2026-05-15
> 목적: 구현 단계에서 사용할 테스트 도구와 우선 검증 항목을 확정한다.

---

## 1. 테스트 도구 선택

AIConnect 초기 구현의 테스트 도구는 다음으로 확정한다.

```text
Vitest + React Testing Library
```

선택 이유:

- 현재 프로젝트가 Vite 기반이라 Vitest를 붙이기 쉽다.
- React Testing Library는 실제 사용자가 보는 화면 기준으로 테스트하기 좋다.
- 기획에서 중요한 정책을 자동으로 확인할 수 있다.
- Jest보다 Vite 프로젝트에서 설정이 가볍다.

---

## 2. 테스트의 역할

테스트는 단순히 오류를 찾는 도구가 아니라, AIConnect의 핵심 정책이 실수로 깨지지 않게 막는 장치다.

예시:

```text
요구사항 작성 화면에는 연락처 입력칸이 없어야 한다.
제안서 승인 전에는 작업 진행표가 열리면 안 된다.
완료된 작업에만 리뷰 작성 버튼이 보여야 한다.
```

이런 규칙을 테스트로 고정해두면, 나중에 기능을 수정해도 중요한 정책이 유지된다.

---

## 3. 우선 테스트 항목

초기 구현에서 반드시 테스트할 항목은 다음과 같다.

### 3.1. 상품 탐색

- 상품 카드에 상품명, 시작가, 작업 기간, 사용 AI 도구가 보인다.
- 상품 카드의 `패키지로 의뢰하기` 버튼이 요구사항 작성 화면으로 연결된다.
- 상품이 없을 때 빈 상태 문구가 보인다.

### 3.2. 요구사항 작성

- 원하는 결과물, 작업 목적, 참고자료, 마감 희망일을 입력할 수 있다.
- 단일 진행과 단계별 진행을 선택할 수 있다.
- 연락처 입력 필드는 존재하지 않는다.
- 외부 연락처로 보이는 문구를 입력하면 경고가 보인다.

### 3.3. 거래 제안서

- 제안서에 금액, 작업 범위, 작업 기간, 산출물이 보인다.
- 제안서 유효기간 3일 안내가 보인다.
- 승인, 수정 요청, 취소 버튼이 보인다.
- 만료된 제안서는 승인할 수 없다.

### 3.4. 작업 진행표

- 제안서 승인 후 작업 진행표가 열린다.
- 단계 상태가 대기, 진행 중, 제출됨, 수정 요청, 승인됨으로 표시된다.
- 전문가가 산출물 링크를 제출할 수 있다.
- 의뢰자가 단계 승인 또는 수정 요청을 할 수 있다.

### 3.5. 마이페이지

- 의뢰자는 내 의뢰 요청, 받은 제안서, 진행 중인 작업을 볼 수 있다.
- 전문가는 내 상품, 받은 의뢰 요청, 보낸 제안서를 볼 수 있다.
- 완료된 작업에만 리뷰 작성 버튼이 보인다.

---

## 4. 테스트 파일 위치

권장 구조:

```text
src/
  test/
    setup.ts
  components/
    ProductCard.test.tsx
  pages/
    Home.test.tsx
    Category.test.tsx
    ServiceRequest.test.tsx
    Proposal.test.tsx
    Workroom.test.tsx
    MyPage.test.tsx
```

공통 테스트 설정은 `src/test/setup.ts`에 둔다.

---

## 5. 구현 단계 적용 방식

기능을 만들 때마다 다음 순서를 따른다.

```text
1. 실패하는 테스트를 먼저 작성한다.
2. 테스트가 실패하는 것을 확인한다.
3. 최소한의 코드로 테스트를 통과시킨다.
4. 관련 테스트와 빌드를 다시 확인한다.
5. 다음 기능으로 넘어간다.
```

이 순서가 하네스 엔지니어링의 3단계인 TDD 기반 구현에 해당한다.

---

## 6. 나중에 추가할 테스트

초기에는 화면 테스트를 우선한다. Supabase 연결 이후에는 다음 테스트를 추가한다.

- RLS 정책 테스트
- Storage 접근 권한 테스트
- 제안서 만료 처리 테스트
- 외부 연락처 필터링 테스트
- 리뷰 작성 조건 테스트

---

## 7. 결론

AIConnect는 거래 정책이 중요한 서비스이므로, 테스트는 선택 기능이 아니라 핵심 안전장치다.

초기 테스트 도구는 다음으로 확정한다.

```text
Vitest + React Testing Library
```

실제 설치와 설정은 구현 시작 직후 첫 작업으로 진행한다.
